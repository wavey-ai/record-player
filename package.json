{
  "name": "vin.yl.player",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "bench:worklet": "npm run build && node scripts/benchmark-worklet.mjs",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev-server.mjs",
    "test:browser": "npm run build && node scripts/browser-smoke.mjs",
    "validation:analyze": "node scripts/analyze-dj-validation.mjs",
    "validation:template": "node scripts/analyze-dj-validation.mjs --template"
  }
}
